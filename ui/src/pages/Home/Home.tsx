import React from "react";
import { motion } from "framer-motion";

import "./home.css";

export const Home = () => {
  return (
    <motion.div className="home-page">
      <div>HOME</div>
    </motion.div>
  );
};
